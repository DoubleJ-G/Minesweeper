name: Setup pnpm

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v2
      with:
        version: 11

    - uses: actions/setup-node@v4
      with:
        node-version-file: ".nvmrc"
        cache: "pnpm"

    - run: pnpm install
      shell: bash
